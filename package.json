{
  "name": "skellock",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.mjs",
    "package": "npm run validate && sh scripts/package.sh"
  }
}
