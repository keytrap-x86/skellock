{
  "name": "sitelock",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.mjs",
    "package": "npm run validate && sh scripts/package.sh"
  }
}
